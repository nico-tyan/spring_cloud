server:
  port: 7000
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          force-pull: true
          uri: https://github.com/nico-tyan/cloud_config/     # 配置git仓库的地址
          #searchPaths: '*.yml'
          username:       # git仓库的账号
          password:             # git仓库的密码
          
