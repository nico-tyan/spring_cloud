spring:
  cloud:
    config:
      name: feign-ribbon #需要从github上读取的资源名称，注意没有yml后缀名
      profile: test   #本次访问的配置项
      label: master 
      #  spring-cloud-config服务器URL
      uri: http://nico.com:7000  #本微服务启动后先去找7000号服务，通过SpringCloudConfig获取GitHub的服务地址
 

